import java.util.ArrayList;

/**
 * This class includes a utility method for checking that two lists match.
 * 
 * @author YOUR_NAME_HERE. Created Sep 30, 2008.
 */
public class ListMatcher {
	/**
	 * Checks whether the two given lists contain the same number of the same
	 * values, ignoring the order. That is, if the first list contains two 3s
	 * then the second list must also contain two 3s or they don't match.
	 * 
	 * Your code MUST NOT CHANGE THE GIVEN LISTS.
	 * 
	 * @param listOne
	 * @param listTwo
	 * @return whether the lists match
	 */
	public static boolean areMatching(ArrayList<Integer> listOne,
			ArrayList<Integer> listTwo) {
		// TODO, Prob. C3: Implement the areMatching method.
		return false;
	}
}
